# CLAUDE.md — 住之江観戦ツール開発（suminoe_kyouteiApp）

## このプロジェクトは何か

2026-08-09（日）のボートレース住之江ナイター観戦（優勝戦日）に向けた**2つのツール**。

1. **スミノエ・リード** (`tools/suminoe-read/`): Python CLI。
   - `main.py` 当日朝に全12Rの出走表を分析して md/csv/json 化
   - `review.py` レース終了後に成績を取得し、事前の読みと照合して md/json 化
   - `collect-history.py` 過去の成績を蓄積（母数のわかる実績を作るため）
2. **スミノエ・ログ** (`apps/suminoe-log/`): Next.js PWA。現地でAndroid片手操作の観戦記録＋賭式別の買い目表示

2ツールは JSON 1本で緩くつながる。リードが `apps/suminoe-log/public/racecard.json` に書き出し、
アプリが起動時に自動で読む（貼り付けは予備手段として残してある）。

**公開URL: https://suminoe-log.vercel.app**
**実装・デプロイは 2026-08-07 に完了。残っているのは Android 実機での確認（ユーザー作業）。**

## ドキュメント

1. [docs/00-overview.md](docs/00-overview.md) — 全体像・スケジュール・リスク登録簿
2. [docs/01-suminoe-log-spec.md](docs/01-suminoe-log-spec.md) — ログ確定仕様 v1.3
3. [docs/02-suminoe-read-spec.md](docs/02-suminoe-read-spec.md) — リード確定仕様 v1.1
4. [docs/03-bangumihyou-format.md](docs/03-bangumihyou-format.md) — 番組表フォーマット検証済みレポート

**確定仕様が正。** `docs/original-prompt-*.md` は受領時の元仕様で、参照用に保存してあるだけ。

## 設計思想（両ツール共通・絶対に崩さない）

**勝つための予想ツールではない。** 舟券の控除率は約25%あり、データ分析でそれを覆す想定はしない。
目的は「根拠を持ってレースを見られること」。

- **検証していない確率を出さない。** 確率を出すなら過去データで較正し、母数と誤差を併記する
  （`public/calibration.json`。743レース・63開催日で検証済み）
- 母数の小さい集計値には必ず母数を併記する
- 買い目は「その賭式で買うならこういう組み方」という**型の提示**に留める。推奨ではない
- **期待値が1.0を下回るときは、下回ったまま出す。** 数字を作って100%超に見せない
- **金額は「実際に買ったもの」だけ扱う。** 2026-08-09 に方針を変えた。
  それまでは金額を一切出さなかったが、自分が何をいくら買って配当がいくらだったかを
  残せないと観戦記録として用をなさない、というユーザーの判断による。
  扱うのは `lib/bets.ts` だけで、**推奨金額や資金配分は出さない**（そこは変えていない）
- 見送りレースでは「買わない」を先に提示する

この方針はテストで機械的に検査している（断定的表現の混入チェック）。緩めないこと。

> 2026-08-09 更新。従前は「存在しない精度の的中確率を出さない」だったが、
> 期待値を出す以上、確率は避けられない。**検証したうえで誤差ごと見せる**方針に変えた。

## 確率と期待値の扱い（触る前に必ず読む）

**期待値の高い買い目を選ぶ操作は、モデルが市場より高く評価している組み合わせを選ぶ操作と同じ。**
モデルの誤差がそのまま「期待値」として現れる。8/9 の前売りオッズで試したとき、
期待値が 2〜7 という数字が並んだが、これはモデルが正しいからではない。

事実として分かっていること（`scripts/calibrate.ts` の出力）:

- オッズは正常。Σ(1/オッズ) は 1.335 で、控除率25%（1/0.75 = 1.333）と一致する
- **1着確率の較正は良好。** 母数の大きいビンで予測と実測の差は ±2pt 以内
- **2着以降は1着と別の温度で較正する**（1着 2.0 / 2-3着 3.0）。同じ温度を使うと
  外枠の2着を高く見すぎ、3連単の期待値が跳ね上がる。分けたことで
  3連単確率の実際÷予測が 0.90 → 1.06 に改善した
- **確率の高い順に買ったときの回収率は控除率どおり**（743レース・総額89万円で検証）

| 買い方 | 的中率 | 回収率 |
|---|---|---|
| 3連単 上位6点 | 29.2% | 74.7% |
| 3連単 上位12点 | 46.8% | 75.7% |
| 3連複 上位3点 | 44.1% | 69.2% |
| 3連複 上位5点 | 61.4% | 71.2% |

控除率25%（＝回収率75%）とほぼ一致する。**モデルは市場と同等の精度はあるが、
控除率を超えてはいない。** この表は画面にも出しているので、消さないこと。

したがって:

- 期待値は「モデルが正しければ」の条件付きの数字。**画面では必ず較正の注記とセットで出す**
- **市場との乖離に上限を設けている**（`MAX_EDGE_RATIO = 3`）。
  モデルが市場の3倍を超えて強気な買い目は選ばない。市場は数千万円の投票が集約された
  見立てで、こちらは63開催日のモデル。10倍の差がついたときはモデル側の誤りを疑う。
  この上限を入れる前は期待値7.2倍という買い目が出ていた
- 較正を通していない量に補正係数を足さない。ズレの原因が分からなくなる
- しきい値（`LONGSHOT_MIN_ODDS`、`MAX_EDGE_RATIO`）を動かすときは、動かした理由を残す

## 実装時の絶対ルール

- **番組表の桁位置を再推測しない。** `docs/03` §6 の確定表（実データ720行でエラー0件）に従う
- **直前情報の HTML セレクタを勘で直さない。** `tests/fixtures/beforeinfo_*.html` が正。
  壊れたらまず fixture を取り直して構造を確かめる。
  **出走表の各行には「その選手の前走」欄があり、そこの艇番セルにも艇色 class が付く**。
  tbody ごとに最初の1つだけを艇番として採ること（素朴に拾うと1レースが12艇に膨らむ。
  1R は前走が無いので露見せず、9R の fixture が回帰テストになっている）
- LZH解凍は `C:\Windows\System32\tar.exe`（bsdtar）を第一手段にする。**フルパス必須**（Git BashのGNU tarはLZH非対応）
- 番組表の当日分は**当日朝5時台公開**。翌日分を取ろうとすると404になる（正常）。
  節の翌日の番組はその日のレースが終わってから組まれるため、前日には公式サイトにも存在しない。
  **当日の昼に選手変更などで再更新されることがある**（8/7分が8/7 12:10に更新された実例）
- ログの Service Worker は**手書きしない**。`scripts/build-sw.mjs` がビルド出力を走査して生成する。
  Next.js はアセット名にハッシュを付けるため、一覧を手書きすると必ずズレてオフラインで壊れる
- **当日変わるデータ（`odds` / `tenji` / `racecard` / `results` / `calibration`）はキャッシュ優先にしない。**
  骨格（HTML/JS/CSS）は名前にハッシュが付くのでキャッシュ優先で正しいが、これらは
  名前が変わらないまま中身だけ差し替わる。キャッシュ優先だと端末は最初に読んだ版を
  永久に返し、締切前にオッズが動いても画面が変わらない。しかもキャッシュ名は
  **アセット名一覧のハッシュ**なので、データだけの更新では名前が変わらず古いキャッシュも消えない。
  `build-sw.mjs` の `LIVE_DATA` が該当パスを通信優先にしている（5秒でキャッシュに落ちるので
  オフライン要件は保たれる）。**8/9 の現地で実際に踏んだ**（サーバーは最新、端末だけ数時間前）
- **`apps/suminoe-log/vercel.json` を消さない。** `framework: null` / `outputDirectory: "out"` が必要。
  Vercel の Next.js ビルダーは `.next` から独自に出力を組み立てるため、`next build` の後に
  `out/` へ足した `sw.js` を拾わず、本番で `/sw.js` が404になる（2026-08-07 に実際に踏んだ）
- **Vercel の Root Directory は `apps/suminoe-log`。** 2026-08-10 に GitHub 連携へ切り替えた。
  ここが `.` だとリポジトリ直下でビルドが走り、`app` ディレクトリが無くて必ず落ちる（実際に踏んだ）。
  **リポジトリ直下に `vercel.json` を置いて回避しない。** ヘッダー設定が二重になり、
  `/sw.js` の 404 という一番踏みたくない不具合を、二箇所を見ないと直せなくなる
- ログの外部通信は**同一オリジンの `/racecard.json` だけ**。外部APIは呼ばない。
  取得できなければ貼り付けにフォールバックする。SWがキャッシュするのでオフラインでも読める
- Python の標準出力は UTF-8 に reconfigure（PowerShellでのUnicodeEncodeError実例あり）
- 買い目の点数・組み方は**リードとログで揃える**（同じデータから違う買い目が出ると混乱する）

## 金額の扱い（`lib/bets.ts`）

**1円の誤差も作らない。** 記録として残る数字なので、丸めも近似もしない。

- 公式の払戻（`results.json` の payouts）は**100円あたりの金額**。
  実際の配当は `払戻 × 買った金額 ÷ 100`。100円単位でしか買えないので割り切れる
- **結果が出ていないレースは `settled: false`** にして払戻を確定させない。
  「まだ分からない」と「外れた」を同じ0円にしない
- **回収率は結果が出たぶんだけを分母にする。** 結果待ちを含めると、
  買った直後に「回収率0%」と出て外れたように見える
- **日付が一致する結果だけで精算する。** `results.json` は前日のものが残っていることがあり、
  検査を忘れると別の日の着順で配当が出る（8/9 の購入に 8/8 の 1-4-6 を当てて
  「的中」と表示された。`app/page.tsx` の `myBets` で日付を見ている）
- 賭式ごとに着順の扱いが違う。3連単・2連単は順番まで、それ以外は順不同で照合する

## ローカル確認で古い画面が出るとき（2026-08-09 に3時間溶かした）

**別プロジェクトの Service Worker がポートを乗っ取っていることがある。**

localhost:3000 に `kobetsu-sien-v2`（個別支援計画アプリ）の SW が登録されたままで、
このアプリのリクエストを横取りし、古いキャッシュを返していた。
症状は「タブを押しても切り替わらない」「編集が反映されない」で、
コードを疑って延々と調べることになる。

確認と解除:

```js
await navigator.serviceWorker.getRegistrations()   // スコープを見る
// 解除
for (const r of await navigator.serviceWorker.getRegistrations()) await r.unregister();
for (const k of await caches.keys()) await caches.delete(k);
```

**ローカル確認は 4321（`.claude/launch.json` の suminoe-log-static）を使う。**
3000 は他のプロジェクトと衝突する。

## 日付の扱い（特定の1日の専用アプリではない）

扱う開催日は**出走表の日付**で決まる（なければ端末の今日）。定義は `lib/raceDate.ts`。

- 記録・下書きは**日付ごとのキー**（`suminoe-log-20260809`）。日が変われば別の記録になる
- 出走表・結果は**日付を含めないキー**（`suminoe-racecard`）。「いま扱う1件」だけ持つ。
  日付付きにすると「日付を知るために日付が必要」という循環になる
- 新しい出走表がデプロイされると、起動時に自動で日付が切り替わり、その日の記録に移る
- **日付をハードコードしない。** 以前 `EVENT_DATE_LABEL = '8/9(日)'` を固定していて、
  8/7 のデータを表示しているのに 8/9 と出る不具合があった

## デザイン（触る前に読む）

方向は「**競艇新聞**」。現地で読むものは新聞である、という一点から組んでいる。
白地に墨、差し色は朱。角丸を使わず、太罫でセクションを切り、細罫で行を分ける。
余白は詰める（紙面は情報密度が価値）。定義は `app/globals.css` 冒頭。

- **テーマは2つ。** light（既定・生成りの紙に墨）と dark（ナイター・墨地に白抜き）。
  ヘッダーのトグルで切替。元仕様の「夜間の屋外でまぶしくならない」要件は dark が担う
- 色は `:root` と `:root[data-theme='dark']` の CSS 変数で定義。**両方に値を足すこと**
- **角丸は使わない。** `@theme` の `--radius-*` をすべて 0 にして、Tailwind の
  `rounded-*` ごと潰してある。個別のクラスを消して回る必要はない
- **`@theme` ブロックを消さない。** ここが色トークン（`--color-bg-deep` など）の定義で、
  消すと `bg-bg-deep` のようなクラスが丸ごと効かなくなる（実際に一度やって、
  ヘッダーの背景が透明になった）
- accent（朱）は「ここぞという1点」にだけ置く。全体に散らすと新聞に見えない
- **枠色ボタンには必ず輪郭（`.boat-edge`）を付ける。** light では1号艇の白が紙に埋没する
- 見出しは `.paper-heading`（墨帯に白抜き）。太罫は `.rule-top`
- **`main` の `px-1.5` と セクションの `px-2` を動かさない。**
  幅360pxで枠色ボタンが48px角を割り、横スクロールが発生する（実測で確認済み）
- **出走表テーブルに `min-width` を置かない。** 幅360pxの実測で、展示の列を足した
  7列でも307pxに収まり折り返しも起きない。以前の `min-w-[22rem]` は、収まる内容を
  352pxに引き伸ばして横スクロールを作っていた

### 期待度の色（パチンコの信頼度表示から借りている）

青 → 緑 → 赤 → 金 → 虹。`lib/patterns.ts` の `heatOf()` が的中率から段を決め、
買い目カードの左端を走る帯（`.heat-bar`）と文字色（`.heat-text-*`）に出る。

- **言葉だけを出さない。** 「激熱」の横に必ず「= 的中率30%以上」を併記する。
  定義のない煽り文句は、回収率74.7%という実測と正面から矛盾する。
  定義があれば、競馬新聞の ◎ ○ ▲ と同じ「基準を持った印」になる
- **色だけで判断できる作りにしない。** 数字と言葉を必ず添える（色覚特性への配慮）
- **虹は動かさない。** `background-position` のアニメーションは毎フレーム描き直しになり、
  ナイター4時間の電池に響く。静止していても虹は十分目立つ
- 段の切り方は実際に出る値の幅に合わせてある（3連複3点は25〜60%、3連単6点は10〜30%）。
  3連単で虹が出ることはまず無い。それが事実なので、出やすくするために閾値を下げない

### 水面のデザインは 2026-08-09 に撤去した

`components/WaveScene.tsx` / `public/scene/` / `scene_assets/` / `tools/build-scene-assets.py`
は残っているが、どこからも参照していない。戻すには page.tsx で `<WaveScene />` を描き、
`globals.css` に `.scene-*` 一式を戻す（コミット 31eae97 以前の履歴にある）。

撤去の理由は方向転換であって、出来が悪かったからではない。
当時の知見（置き場所を2回間違えた経緯、素材を「画像を貼っただけ」に見せない処置）は
git 履歴の globals.css コメントに残してある。

## 自動化（Windows タスクスケジューラに登録済み）

**2026-08-10 から GitHub Actions へ移行中**（`.github/workflows/collect.yml`）。
狙いは PC の起動に依存しないこと。タスクスケジューラだと、PCが落ちている日の
オッズが永久に取れない。デプロイは **Vercel の Git 連携**が担う（main への push が引き金）。
**デプロイ用トークンは持たない。** 長期間有効な資格情報を1つ抱えずに済むため。

以下は移行前からある Windows タスク。両方が同時に走っても冪等なので壊れないが、
GitHub Actions 側が安定したら止める。

| タスク名 | 起動 | 内容 |
|---|---|---|
| `Suminoe-RaceCard-Refresh` | 8/9 5:10〜 | 番組表を取得しアプリを更新してデプロイ |
| `Suminoe-Tenji-0809` | 8/9 12:00〜 | 直前情報（展示タイム）**とオッズ**を取得しアプリを更新してデプロイ |
| `Suminoe-Result-Review-0807` | 8/7 21:30〜 | 8/7の成績を取得し事前の読みと照合してデプロイ |
| `Suminoe-Result-Review-0809` | 8/9 21:30〜 | 8/9の成績で同じことをする（観戦後の振り返り） |

refresh と review は45分おきに8時間、**tenji だけ15分おきに10時間**繰り返す。
tenji の間隔が短いのは、直前情報とオッズがレースごとに順次動くため
（他の2つは「1日1回どこかで取れれば終わり」）。

**8/9 に 30分 → 15分 へ縮めた。** レース間隔が約28分なので、30分間隔だと
締切時点で30分前のオッズを見ることがあり、期待値がその分ずれる。
オッズは締切直前まで動く量なので、ここは取得間隔がそのまま精度になる。

- 実体: `refresh-and-deploy.sh` / `tenji-and-deploy.sh` / `review-and-deploy.sh`
  （`.cmd` がラッパー。**CRLF必須**）
- **冪等**。refresh/review はすでに同じ日付が入っていれば何もしない。
  tenji は**前回と中身が同じならデプロイしない**（同じ日に何度も更新されるため、日付では判定できない）。
  取り直すには `FORCE=1`
- `StartWhenAvailable=true` なので、時刻にPCが起動していなくても**起動後すぐ実行**される
- ログ: `tools/suminoe-read/logs/{refresh,tenji,review}-<date>.log`
- 作り直すには `python make-task-xml.py --action tenji --date 2026-08-09 --time 12:00`
  → `schtasks /create /xml "...task-tenji.xml" /tn "<名前>" /f`

**データの公開タイミング（実測）**
- 番組表(B): 当日早朝5時台。当日昼に再更新されることがある
- 成績(K): **全レース終了後**（8/6 は 20:53 JST）。レース前は中身が空の300バイトで存在するため、
  `MIN_CONTENT_BYTES` でサイズを見て弾いている
- 直前情報(HTML): 各レースの**周回展示後**（締切の10〜15分前）に順次。
  未公開でもページは 200 で返り、値が空なだけなので、
  **展示タイムが1つも取れないこと**をもって「まだ」と判定している

## 環境（2026-08-07 確認済み）

- Windows 11 / PowerShell 7 / Git Bash 併用
- Node v24.15.0 / npm 11.12.1 / Python 3.14.6 / git 2.54
- 7-Zip **なし** / bsdtar 3.8.4 **あり**（System32、LZH解凍を実機確認済み）
- Vercel CLI **未インストール**（デプロイ時に `npm i -g vercel` が必要）

## コマンド

```bash
# スミノエ・ログ（apps/suminoe-log/ 内）
npm run dev          # 開発サーバー
npm run build        # 静的エクスポート + SW生成 → out/
npm test             # 143ケース（vitest）
npm run typecheck
npm run lint
npm run icons        # PWAアイコン再生成

# スミノエ・リード（tools/suminoe-read/ 内、PowerShell）
.\venv\Scripts\Activate.ps1
python main.py --date 2026-08-06 --dry-run --local-file tests\fixtures\B260806.TXT
python beforeinfo.py                 # 直前情報（全12R）→ public/tenji.json
python beforeinfo.py --races 1-3     # レースを絞る（公式へは1.5秒間隔）
python collect-cards.py              # 較正用に過去の番組表を集める → cache/cards/
python -m pytest     # 94ケース（ネット接続不要）
```

Git Bash から Python を叩くときは venv の実行ファイルを直接指定する:
`./venv/Scripts/python.exe -m pytest`（`PYTHONIOENCODING=utf-8` を付けると日本語が読める）

ローカルでビルド出力を確認するには `.claude/launch.json` の `suminoe-log-static`（port 4321）を使う。

TypeScript のスクリプトは MCP の tsx を借りて動かす（アプリ側に tsx を入れていない）。

```bash
# リポジトリのルートから
./tools/suminoe-mcp/node_modules/.bin/tsx tools/suminoe-mcp/scripts/fetch-odds.ts   # オッズ → public/odds.json
./tools/suminoe-mcp/node_modules/.bin/tsx tools/suminoe-mcp/scripts/build-closing-odds.ts  # 締切直前のオッズ → archive/
./tools/suminoe-mcp/node_modules/.bin/tsx apps/suminoe-log/scripts/calibrate.ts     # 較正 → public/calibration.json
./tools/suminoe-mcp/node_modules/.bin/tsx apps/suminoe-log/scripts/preview-patterns.ts  # 全12Rの買い方を一覧
```

## 過去成績の蓄積（母数のわかる実績）

番組表の当地勝率・M2率は**走数が公開されていない**ため母数不明で、断定に使えない。
そこで過去の競走成績を自分で集計して母数付きの実績を作っている。

```bash
python collect-history.py --days 150   # cache/history/ に蓄積（1.5秒間隔）
```

- **モーターは母数が集まる**（場に固定。65機すべてが12走以上）→ 番組表値との乖離を注記に出す
- **選手は母数が集まりにくい**（遠方の選手は年数回しか来ない）→ 10走以上の選手だけ実測を混ぜる
- 実測の混ぜ方は `suminoe/enrich.py`。`Racer.history_shoritsu` に入れるだけなので
  判定ロジック（analyzer）は触らない設計
- **判定は番組表の値で行い、実測は判断材料として併記する**。二重に効かせて過学習させない

**オッズは過去に遡って取れない。** 取れたときに必ず残す。

- 15分おきのスナップショット → `cache/odds/<日付>/<HHMM>.json`（git 管理外）
- そこから**締切以前の最後の1枚**を抜き出して `public/archive/closing-odds-<日付>.json` に置く。
  夜の照合タスク（全レース終了後）が作る
- **締切を過ぎた後のオッズは使わない。** 買う時点では見えていないので後知恵になる
- これが貯まると「その買い方で買っていたらどうだったか」を過去に遡って測れる。
  8/9 に期待値順と確率順のどちらがマシか判定できなかったのは、これが無かったため

## テストの方針

- リード: pytest。`tests/fixtures/` に実LZH+TXTを同梱しているので**ネット接続なしで全件通る**
- ログ: vitest。`lib/__fixtures__/racecard-20260806.json` と `tenji-20260808.json` はリードの実出力。
  **2ツール間のデータ契約テスト**になっており、リードのJSON形式を変えるとここが落ちる（意図的）
- 直前情報は**公式の競走成績と突き合わせて検証できる**（成績TXTにも展示タイムが載っている）。
  8/8 の72件で一致を確認した。スクレイピングを直したら同じ照合をやり直すこと

## 進捗

- [x] 8/7: 仕様確定・番組表フォーマット実地検証・プロジェクト構造整備
- [x] 8/7: スミノエ・リード実装（33テスト、8/6実データで判定分布を検証、サンプル出力同梱）
- [x] 8/7: スミノエ・ログ実装（30テスト、ビルド成功、**サーバー停止状態で全4タブ動作を実証**）
- [x] 8/7: 買い目タブ追加（ユーザー要望による仕様変更 R10）
- [x] 8/7: Vercel 本番デプロイ（https://suminoe-log.vercel.app）
- [x] 8/7: デザイン刷新（R11）・出走表の同梱（R12）・当日の自動更新をタスク登録
- [x] 8/7: 結果照合機能（成績パーサ・照合・アプリ表示）と夜間の自動照合タスク
- [x] 8/7: 過去成績の蓄積（21開催日240レース）とモーター実測の併記
- [x] 8/7: 収支タブ（賭式ごとの当選率・1点100円での回収率）
- [x] 8/7: 展示の反映・締切の自動選択・レース連動・結果欄の折りたたみ
- [x] 8/7: 明るいテーマ＋ナイター切替、波しぶきと艇のシルエット、日付固定の撤去
- [x] 8/9: 直前情報の取得と表示（展示タイム・チルト・部品交換・スタート展示・水面気象）。
      公式成績の展示タイム72件と突き合わせて一致を確認。`Suminoe-Tenji-0809` を登録・本番デプロイ済み
- [x] 8/9: **現地観戦。全12レースを実運用した。** ここで出た不具合と気づきが以下
- [x] 8/9: SW が当日変わるデータをキャッシュ優先で返していたのを修正（端末だけ数時間古かった）
- [x] 8/9: 直前情報とオッズの取得を30分→15分間隔に（締切時点で平均9分前のオッズになった）
- [x] 8/10: 見（ケン）が次のレースに波及する不具合を修正。レース移動で中身を持ち越さない
- [x] 8/10: 「勝負」を期待値順から確率順に戻す（期待値順は未検証だった）。「堅実」にも乖離の上限
- [x] 8/10: 当たっても損な点に印／金額を 1〜30 × 百千万／オッズ専用タブ／型ごとの実測表示
- [x] 8/10: 締切直前のオッズを archive に残す（`closing-odds-*.json`）
- [ ] クラウド移行（設計は `docs/superpowers/specs/2026-08-10-cloud-accumulation-design.md`）。
      **アカウント作成とトークン登録がユーザー作業として必要なため未着手**
- [ ] Android 実機で機内モード検証（仕様 01 §8 のQAチェックリスト）

## しきい値を触るときの注意

分析の判定しきい値は 8/6 の実データで検証して調整した記録がある。変更する前に読むこと。

- リード: `tools/suminoe-read/README.md`「しきい値の調整記録」（7件の修正履歴）
- ログ: `lib/betting.ts` 冒頭のコース補正の定数コメント（`COURSE_WEIGHT_FIRST = 14` の根拠）

特にコース補正は、強すぎると「A1トップが外にいても常に1号艇が軸」になり、
弱すぎると「住之江の1コース1着率56.2%を無視した買い目」になる。両極を避ける値にしてある。
